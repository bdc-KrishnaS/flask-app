pipeline {
    agent any

    stages {
        
        stage('Deploy to Conatiner') {
            steps {
                // bat "sudo su juser"
                // bat " cp /home/user/docker-compose/docker-compose.yml . "
                bat "docker-compose down "
                bat "docker-compose up --build -d"
            }
        }
    }
}
